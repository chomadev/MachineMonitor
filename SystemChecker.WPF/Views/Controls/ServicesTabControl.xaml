<UserControl x:Class="SystemChecker.WPF.Views.Controls.ServicesTabControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ListView ItemsSource="{Binding Services}"
              BorderThickness="0">
        <ListView.View>
            <GridView>
                <GridViewColumn Header="Name" DisplayMemberBinding="{Binding Name}"/>
                <GridViewColumn Header="Status" DisplayMemberBinding="{Binding Status}"/>
                <GridViewColumn Header="Running">
                    <GridViewColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding IsRunning, Converter={StaticResource BooleanToTextConverter}}"
                                     Foreground="{Binding IsRunning, Converter={StaticResource IsRunningColorConverter}}"/>
                        </DataTemplate>
                    </GridViewColumn.CellTemplate>
                </GridViewColumn>
            </GridView>
        </ListView.View>
    </ListView>
</UserControl> 
<UserControl x:Class="SystemChecker.WPF.Views.TcpPortsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="clr-namespace:SystemChecker.WPF.Converters">
    
    <UserControl.Resources>
        <converters:TcpPortStatusConverter x:Key="TcpPortStatusConverter"/>
    </UserControl.Resources>
    
    <ListView ItemsSource="{Binding TcpPortStatuses}">
        <ListView.View>
            <GridView>
                <GridViewColumn Header="Port" DisplayMemberBinding="{Binding Port}" Width="60"/>
                <GridViewColumn Header="Status" Width="150">
                    <GridViewColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Converter={StaticResource TcpPortStatusConverter}}"
                                     Foreground="{Binding Converter={StaticResource TcpPortStatusConverter}}"/>
                        </DataTemplate>
                    </GridViewColumn.CellTemplate>
                </GridViewColumn>
            </GridView>
        </ListView.View>
    </ListView>
</UserControl> 